{
  "name": "vercel-mpd-player",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "A Vercel-ready DASH/HLS player with a Paris-pinned, IPv4-first streaming proxy.",
  "scripts": {
    "check": "node --check app.js && node --check api/proxy.js",
    "vercel-build": "echo Static player: no build step required"
  },
  "engines": {
    "node": ">=20"
  }
}
