{
  "name": "vercel-mpd-player",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A Vercel-ready DASH/HLS player using Shaka Player with an allowlisted proxy fallback.",
  "scripts": {
    "check": "node --check app.js && node --check api/proxy.js",
    "vercel-build": "echo Static player: no build step required"
  },
  "engines": {
    "node": ">=20"
  }
}
